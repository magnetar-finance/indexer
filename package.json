{
    "name": "indexer",
    "license": "UNLICENSED",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ cedarfi/indexer",
        "goldsky:deploy": "goldsky subgraph deploy",
        "create-local": "graph create --node http://localhost:8020/ cedarfi/indexer",
        "remove-local": "graph remove --node http://localhost:8020/ cedarfi/indexer",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 cedarfi/indexer",
        "test": "graph test",
        "format": "prettier --write ."
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.95.0",
        "@graphprotocol/graph-ts": "0.36.0",
        "source-map-support": "^0.5.21"
    },
    "devDependencies": {
        "matchstick-as": "0.6.0"
    }
}
